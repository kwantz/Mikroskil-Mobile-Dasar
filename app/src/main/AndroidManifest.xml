<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kwantz.mobiledasar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/gp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomepageActivity"
            android:label="@string/title_activity_homepage" />
        <activity
            android:name=".SearchActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity android:name=".DeliveryActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".BarangActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".DetailTagihanActivity"></activity>
    </application>

</manifest>